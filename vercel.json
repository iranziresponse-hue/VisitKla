{
  "installCommand": "npm --prefix web install",
  "buildCommand": "node scripts/sync-web-data.js && npm --prefix web run build",
  "outputDirectory": "web/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
